- name: Installing arch packages
  pacman:
    name: "{{ item }}"
    state: present
  with_items: "{{ arch }}"

- name: Installing AUR packages
  aur:
    name: "{{ item }}"
    dir: /opt/aur
    user: "{{ ansible_user }}"
  with_items: "{{ aur }}"

---
- hosts: localhost
  tasks:
    - name: check if key-pair exists
      stat: path=~/.ssh/labradoodle
      register: key_file

    - name: generate SSH key-pair
      shell: ssh-keygen -b 2048 -t rsa -f ~/.ssh/labradoodle -q -N ""
      when: key_file.stat.exists == False

- hosts: labradoodle
  gather_facts: False
  tasks:
    - name: distribute public key
      authorized_key: user="{{ ansible_user }}"
                      key="{{ lookup('file', '~/.ssh/labradoodle.pub') }}"
                      state=present
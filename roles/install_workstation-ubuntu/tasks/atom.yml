# Installing Atom - the hackable text editor
- name: checking if Atom is installed
  become: false
  shell: type "atom"
  register: result
  change_when: no
  ignore_errors: true

- name: installing Atom
  command: snap install --classic atom
  when: result|failed
